\begin{center}
{\LARGE\bfseries \ReportTitle\par}
\vspace{0.8em}
{\large \StudentAuthors\par}
\vspace{0.4em}
{\small \SchoolAffiliation\par}
\vspace{0.4em}
{\small \SupervisingTeachers\par}
\end{center}

\section*{Abstract}
\noindent Air-quality records are used for regulation, public communication,
exposure assessment, and scientific analysis, yet their time series can display
breaks for many reasons. An abrupt shift near a reported measurement-method
transition may reflect a station-specific monitoring discontinuity, but it may
also coincide with weather, smoke, emissions, data processing, or a broader
regional movement. A single monitor cannot distinguish these explanations. This
report introduces MetaShift-Bench, a reproducible audit benchmark for reported
United States Environmental Protection Agency Air Quality System (AQS) PM2.5
Method Code transitions. A transition is treated as a pre-observation metadata
anchor, never as proof of a physical replacement, instrument failure, or causal
measurement bias. The protocol builds geographically constrained
counterfactuals using distinct physical donor sites, tests cross-site and
single-series methods under known synthetic perturbations, and retains every
eligible real anchor with either diagnostics or an explicit machine-readable
failure reason.

\noindent The frozen inventory contains \CanonicalRecords{} canonical daily
observations from \MonitorSeries{} monitor time series and \TotalAnchors{}
persistent anchors. Only \ThreeDonorAnchors{} anchors have at least three
distinct physical donors. Of all anchors, \CompleteComparisons{} receive the
common cross-site comparison, while \InsufficientDonorAnchors{} are
donor-insufficient and \InputFailureAnchors{} have input-window failures. The
stable-regime synthetic benchmark uses \SyntheticCases{} physical target sites:
\CalibrationCases{} calibration sites select frozen decision thresholds, and
\EvaluationCases{} complete-input-disjoint sites supply held-out results.
Standard synthetic control has local-effect MAE \StdSCMAE{}, AUPRC
\StdSCAUPRC{}, macro-F1 \StdSCFOne{}, and regional false-positive rate
\StdSCFPR{}. Cross-validated MetaShift has a lower MAE point estimate
(\CVMAE{}) but lower AUPRC and macro-F1 than standard synthetic control; its
paired MAE difference is \CVPairedMAEDifference{} with 95\% bootstrap interval
[\CVPairedMALower{}, \CVPairedMAEUpper{}].

\noindent The paired interval crosses zero, so the frozen evidence does not
support a general MetaShift superiority claim. On the same held-out design,
fixed-weight conditional bootstrap intervals under-cover at nominal 95\%, while
split-conformal intervals over-cover at nominal 90\%; real-event intervals are
therefore diagnostic rather than calibrated physical-bias confidence intervals.
The audit reports \SupportedCandidates{} supported candidates,
\NotSupportedEvents{} not-supported events, and \InconclusiveEvents{}
inconclusive events under predeclared observational rules. These are not
instrument-fault labels. MetaShift-Bench's contribution is a complete,
metadata-anchored evidence record that distinguishes an analyzable local
residual pattern from unavailable evidence, preserves negative findings, and
makes the limits of a cross-site audit reproducible.

\noindent\textbf{Keywords:} air-quality monitoring; PM2.5; metadata anchors;
synthetic control; change-point attribution; reproducible audit.

\paragraph{Frozen-evidence statement.}
All quantitative findings in this report are generated from the
\EvidenceTag{} evidence release at commit \texttt{\EvidenceCommit{}}; see the
\href{\EvidenceReleaseURL}{official frozen evidence release}. The full
case-manifest SHA-256 is recorded in the frozen evidence configuration and
reproducibility table. This document does not modify the frozen study protocol,
change a held-out result, or reopen model tuning.
